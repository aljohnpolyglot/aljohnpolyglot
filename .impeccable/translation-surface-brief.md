# Translation Services — Center-Stage Engine

## Scope and visitor job

Polish `translation.html` without replacing its identity, content, pricing, case study, form, or shared navbar/footer. This is a Persuade surface: a visitor should understand Aljohn's non-certified translation offer, supported language scope, exclusions, process, proof, price structure, and how to request a quote.

## Approved direction

Center-Stage Engine, selected from surface seed `3af25a4e`. The existing centered promise and full-width moving flag conveyor remain the memorable first-viewport mechanism. The engine becomes larger and proportionate, the hero loses hard-coded offsets, and the same navy/cyan/yellow grammar continues into a calm white service ledger and the existing lower sections.

Approved comp: `.impeccable/mocks/decision/translation-center-stage-engine.png`

The comp's simplified dark header and generated supporting sentences are not literal. Implementation must keep the canonical shared navbar/footer and current factual copy.

## Sampled visual record

- Hero navy field: `#091A37` to `#071832`; deeper belt field: `#022345`.
- Primary action: `#F7E300`; service ledger: `#F4F6FA` to `#F5F6F9`.
- Signal accent: cyan `#00A9FF`; sparse site red remains `#C8102E` where existing semantics require it.
- Display: high-contrast editorial serif, compact line-height; body and controls: the site's system sans.
- Corners: mostly square; CTA and flag tiles softly eased; engine glass approximately `0.5rem`.
- Lines: quiet `1px` ledger rules, `2px` cyan engine border; elevation is reserved for the engine's controlled glow.

## Implementation inventory

| Ingredient | Commitment | Medium |
| --- | --- | --- |
| Shared navbar/footer | Existing canonical templates, unchanged | Existing HTML loaders/components |
| Hero headline and quote action | Centered, readable measure; yellow CTA above conveyor | Semantic HTML/CSS |
| Plus-sign navy field | Dense but low-contrast across hero | CSS data pattern |
| Flag conveyor | One full-width continuous belt, deterministic sequence, fluid spacing, reduced-motion pause | HTML/CSS + approved FlagCDN flags |
| Translation engine | Large centered glass panel overlapping belt, one cyan status line | HTML/CSS |
| Service ledger | Three benefits in one ruled horizontal band, not floating cards | Semantic HTML/CSS + existing icon font |
| Scope, case study, process, pricing, form | Preserve all current facts and functionality inside the same restrained system | Semantic HTML/CSS; existing document image and embeds |
| New generated shipping raster | None; the mock is specification only | Accepted omission |

## Constraints

Use fluid ratios and `clamp()`, never screenshot pixel proportions or device-specific top offsets. Keep visible focus, intrinsic image dimensions, truthful alt text, keyboard-safe embeds, reduced-motion behavior, and no document overflow. Preserve the explicit exclusion of certified/legal translation work and all existing payment/process terms.
