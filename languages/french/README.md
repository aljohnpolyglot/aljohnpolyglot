# French page development

From `languages/french/`, run:

```powershell
npx vite
```

The development server opens `/languages/french/` with the shared site templates and assets available from the repository root.

To create a production bundle in `dist-french/`, run:

```powershell
npx vite build
```
