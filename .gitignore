# Node.js / npm / Vite
node_modules/
dist/
dist-french/
.env
.env.*
*.local

# VS Code editor
.vscode/

# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# SSL Certificates (local development)
localhost-cert.pem
localhost-key.pem
*.crt
*.key

# Design Files (source, not for deployment)
*.psd
*.ai
*.sketch

# Python cache and virtual environments
__pycache__/
*.py[cod]
*$py.class
.Python
venv/
env/
build/
develop-eggs/
eggs/
sdist/
var/
*.egg-info/
.installed.cfg
*.egg
MANIFEST
# If you have specific Python scripts that generate output you don't want committed:
# *.output
# specific_script_output_folder/

# Netlify (even if deleted locally, good to keep if it might reappear)
.netlify/

# Other temporary or sensitive files
*.log
*.tmp
*.swp
*.swo
# Local agent instructions
/AGENTS.md

# Private local curation exports (never deploy or commit)
/local-data/

# Local browser QA caches and disposable captures
/.impeccable/*profile*/
/.impeccable/review/*
!/.impeccable/review/desktop.png
!/.impeccable/review/hero-repro.png
!/.impeccable/review/mobile.png
!/.impeccable/review/mobile-long.png
!/.impeccable/review/creator-modal.png
/.impeccable/mocks/decision/
/.impeccable/mocks/patchwork-cover.png
/.impeccable/mocks/patchwork-cover.prompt.txt
/.impeccable/questions/
/.impeccable/quality/*
!/.impeccable/quality/quilt-hero.webp
